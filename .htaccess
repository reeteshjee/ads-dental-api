RewriteEngine On

# Skip existing files or directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route everything (inside /api) to index.php
RewriteRule ^(.*)$ index.php [QSA,L]
